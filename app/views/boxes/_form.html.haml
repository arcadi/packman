= simple_form_for @box, :html => { :class => 'form-horizontal' } do |f|
  = f.input :name
  = f.input :volume
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), boxes_path, :class => 'btn'
